spring:
  datasource:
    url: jdbc:mysql://localhost:3306/igirepay_gateway?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "ADDk,i!36+seAP*"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
  error:
    include-message: always

app:
  payment:
    processing-delay: 2000
  jwt:
    secret: igirepay-secret-key-must-be-32-chars!!
    expiration: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs